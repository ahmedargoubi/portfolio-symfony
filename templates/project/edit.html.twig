{% extends 'base.html.twig' %}

{% block title %}Modifier le projet{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1><i class="bi bi-pencil"></i> Modifier : {{ project.title }}</h1>

    <div class="card mt-4">
        <div class="card-body">
            {{ include('project/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
    </div>
</div>
{% endblock %}